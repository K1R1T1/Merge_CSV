# Merge_CSV
Python code for merging similar CSV files. 

This is a simple yet effective Python3 code for merging CSV files. 

## How to?
Just add all CSV files you want to merge into the same directory and run the code. 
